name: Flutter CI

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: dart run tool/verify_architecture.dart
      - run: flutter analyze
      - run: flutter test
      - run: flutter build apk --debug --flavor stage
